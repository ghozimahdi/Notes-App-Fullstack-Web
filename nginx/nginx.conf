# Konfigurasi reverse proxy untuk semua environment
server {
    listen 80;

    # Domain/subdomain untuk aplikasi dev
    server_name dev.example.com;

    # Routing untuk frontend
    location / {
        proxy_pass http://frontend_dev:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Routing untuk backend
    location /api/ {
        proxy_pass http://backend_dev:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;

    # Domain/subdomain untuk aplikasi staging
    server_name staging.example.com;

    # Routing untuk frontend
    location / {
        proxy_pass http://frontend_staging:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Routing untuk backend
    location /api/ {
        proxy_pass http://backend_staging:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;

    # Domain/subdomain untuk aplikasi production
    server_name example.com;

    # Routing untuk frontend
    location / {
        proxy_pass http://frontend_prod:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Routing untuk backend
    location /api/ {
        proxy_pass http://backend_prod:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}